# PMS 按钮功能实测分析报告

> 基于逐文件源码分析，按页面、按按钮逐一标注：**✅ 有功能** / **⚠️ 部分实现** / **❌ 空壳** / **🔧 需验证**

---

## 一、项目列表页 (`project/index.vue`)

| 按钮 | 位置 | 状态 | 说明 |
|------|------|------|------|
| **新建项目** | 工具栏 | ✅ | 跳转 `/pms/project-create` |
| **全部/我负责的/我参与的/已归档** | 快捷筛选 | ❌ | `mine`和`involved`筛选逻辑完全为空（TODO注释），点击后列表不变 |
| **批量归档** | 工具栏 | ⚠️ | 有逻辑但需要选中项目，且 `updateProject` API 调用后未处理雪花ID |
| **导出Excel** | 工具栏 | ❌ | 点击后只弹 `message.info('导出Excel功能开发中')` |
| **卡片视图：查看详情** | 卡片点击 | ✅ | `goDetail(project)` → 跳转详情页 |
| **卡片视图：编辑** | 下拉菜单 | ⚠️ | 调用 `ProjectForm.open('update', project)`，需要确认弹窗是否正常弹出 |
| **卡片视图：归档** | 下拉菜单 | ⚠️ | 有确认弹窗 + API调用 |
| **卡片视图：删除** | 下拉菜单 | ⚠️ | 有确认弹窗 + API调用，但 `deleteProject(project.projectId as number)` 使用 `as number` 强制转换，雪花ID精度风险 |
| **列表视图：详情/编辑/删除** | 操作列 | ⚠️ | 同上 |
| **列表视图：多选** | 表格 | ✅ | 支持多选，用于批量归档 |

**关键问题**：
- "我负责的"/"我参与的"两个按钮完全空壳
- "导出Excel"完全空壳
- 项目经理名称显示 `用户${id}` — 未加载用户列表

---

## 二、项目创建页 (`project-create/index.vue`)

| 按钮 | 位置 | 状态 | 说明 |
|------|------|------|------|
| **选择模板卡片** | 步骤1 | ✅ | `selectTemplate(id)` 加载模板阶段和任务 |
| **下一步/上一步** | 底部 | ✅ | 步骤切换 + 表单校验 |
| **取消** | 步骤1 | ✅ | `router.push('/pms/project')` |
| **批量操作-应用** | 步骤3 | ✅ | 支持偏移日期/设责任人/设优先级/设工期 |
| **添加任务** | 步骤3 | ✅ | 弹出任务编辑弹窗 |
| **编辑/删除任务** | 步骤3表格 | ✅ | 编辑弹窗 + 删除确认 |
| **提交创建** | 步骤4 | ✅ | 调用 `createProject` API |
| **草稿恢复弹窗** | 加载时 | ✅ | 24小时有效 |

**关键问题**：
- 步骤2表单：**项目经理**选择器有 `userList` 加载 ✅
- 步骤3批量操作：**批量设责任人**有 `userList` 选择 ✅
- 步骤3表格：**责任人列显示** `row.ownerName || row.roleName || '未指定'` — 如果批量操作未设置ownerName，显示的是roleName（如"PM"），而非真实人名 ⚠️
- 步骤2表单**缺少"协助人"字段** — 创建项目时无法指定项目成员

---

## 三、项目详情页 (`project-detail/index.vue`)

| 按钮 | 位置 | 状态 | 说明 |
|------|------|------|------|
| **复制项目** | 头部 | ❌ | `message.info('复制项目功能开发中')` |
| **编辑** | 头部 | ⚠️ | 跳转 `PmsProject` 带 query edit 参数，需验证ProjectForm是否正确处理 |
| **导出项目** | 更多菜单 | ❌ | `message.info('导出功能开发中')` |
| **分享链接** | 更多菜单 | ✅ | 复制URL到剪贴板 |
| **归档项目** | 更多菜单 | ✅ | 确认弹窗 + API调用 |
| **删除项目** | 更多菜单 | ✅ | 确认弹窗 + API调用 |
| **返回** | 头部 | ✅ | `router.back()` |
| **9个Tab切换** | Tab栏 | ⚠️ | 切换时触发懒加载refresh，但Members/Documents/Approval等子Tab各自独立加载数据 |
| **概览Tab** | Tab内容 | ✅ | 统计卡片/阶段进度/里程碑/最近动态 |
| **任务列表Tab** | Tab内容 | 见下方 | 见TaskListTab详细分析 |
| **看板Tab** | Tab内容 | ⚠️ | 卡片拖拽功能未实现，仅展示 |
| **甘特图Tab** | Tab内容 | ⚠️ | dhtmlx-gantt渲染，拖拽后仅console.log，未调API保存 |

---

## 四、任务列表Tab (`TaskListTab.vue`) — 用户重点反馈

| 按钮 | 状态 | 说明 |
|------|------|------|
| **新建任务** | ✅ | emit `create-task` → 父组件弹出创建弹窗 |
| **接收** | ✅ | `not_started → in_progress` |
| **拒绝** | ⚠️ | `not_started → rejected`，但 `rejected` 状态未在 `taskStatusMap` 中定义，标签会显示原始值 |
| **开始** | ✅ | `not_started → in_progress`（与"接收"同源同目标，语义重叠） |
| **提交完成** | ✅ | 弹出输出物校验弹窗，检查关联文档 |
| **审核通过** | ✅ | `pending_review → completed` |
| **驳回** | ✅ | `pending_review → in_progress` |
| **标记延期** | ✅ | `in_progress → delayed` |
| **恢复** | ✅ | `delayed/paused → in_progress` |
| **发起变更** | ❌ | 仅在 `completed` 状态显示，但 emit `start-change` 事件在父组件中没有处理！点击后无任何效果 |
| **详情** | ✅ | emit `taskClick` → 打开TaskDetailDrawer |
| **展开全部/折叠全部** | ✅ | 树形表格展开折叠 |
| **搜索/筛选** | ⚠️ | 搜索和阶段/状态筛选正常；"我负责的"筛选逻辑为空(TODO)；"我参与的"下拉选项存在但逻辑为空 |

**核心问题**���
1. **"发起变更"按钮完全无效** — emit的事件父组件没有监听
2. **"我负责的"/"我参与的"筛选完全空壳**
3. **负责人显示 `用户${id}`** — TaskListTab 没有加载userList，也没有接收userList prop
4. **`rejected` 状态未定义** — `taskStatusMap` 中没有 `rejected` 的映射

---

## 五、任务详情抽屉 (`TaskDetailDrawer.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **编辑** | ⚠️ | 只设置 `isEditing = true`，但模板中没有任何编辑表单！点击后界面无变化 |
| **进度填报** | ✅ | 弹出进度填报弹窗，可填进度+说明 |
| **进度填报-提交** | ✅ | 调用 `updateTask` API |
| **发起变更** | ❌ | `message.info('变更管理功能开发中')` |
| **暂停任务** | ✅ | 确认弹窗 + API调用 |
| **完成任务** | ✅ | 确认弹窗 + API调用 |
| **上传文件(输出物Tab)** | ❌ | `message.info('文件上传功能开发中')` |
| **查看(输出物Tab)** | ❌ | 只有按钮没有实现 |
| **审核Tab-提交审核** | ⚠️ | 只 `message.success` 但没有调用API！审核意见未发送到后端 |

**核心问题**：
1. **"编辑"按钮点击后无任何界面变化** — 设置了 `isEditing = true` 但模板中没有 `v-if="isEditing"` 对应的编辑表单
2. **"发起变更"按钮空壳**
3. **"上传文件"按钮空壳**
4. **审核提交不调API** — 审核结果仅前端提示，未实际保存
5. **负责人显示 `用户${id}`** — 抽屉组件没有接收userList
6. **参与人显示 `用户${id}`** — `getHelperNames` 硬编码 `用户${id}`

---

## 六、创建任务弹窗 (项目详情页内嵌 - `project-detail/index.vue:169-253`)

| 字段 | 状态 | 说明 |
|------|------|------|
| **任务名称** | ✅ | 必填校验 |
| **所属阶段** | ✅ | 从 `projectStages` 加载 |
| **任务类型** | ⚠️ | 使用硬编码 `taskTypeOptions`，未从字典加载 |
| **优先级** | ⚠️ | 使用硬编码 `priorityOptions`，未从字典加载 |
| **责任人** | ✅ | 有 `userList` 选择器 ✅ |
| **协助人** | ✅ | 多选 `userList` ✅ |
| **计划开始/结束** | ✅ | 日期选择器 |
| **工期** | ✅ | 数字输入 |
| **里程碑** | ✅ | 开关 |
| **任务描述** | ✅ | 文本域 |
| **输出物要求** | ✅ | 文本域 |
| **确定** | ✅ | 调用 `createTask` API |

**用户反馈对照**："创建任务页面没有关联人" — 经核实，**项目详情页内嵌的创建任务弹窗是有责任人和协助人字段的** ✅。但你可能是从**独立任务管理页面** (`task/index.vue`) 创建的任务。

---

## 七、独立任务管理页 (`task/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **新建任务** | ✅ | 弹出创建弹窗 |
| **查询/重置** | ✅ | 筛选功能 |
| **表格/卡片切换** | ✅ | 视图切换 |
| **详情** | ✅ | 打开TaskDetailDrawer |
| **编辑** | ✅ | 打开编辑弹窗（预填数据） |

**创建任务弹窗字段**：
| 字段 | 状态 | 说明 |
|------|------|------|
| 任务名称 | ✅ | |
| 所属项目 | ✅ | 下拉选择 |
| 所属阶段 | ✅ | 根据项目联动 |
| 任务类型 | ✅ | |
| **负责人** | ✅ | 从 `userList` 加载 ✅ |
| 优先级 | ✅ | |
| 计划开始/结束 | ✅ | |
| 工期 | ✅ | |
| 里程碑 | ✅ | |
| 任务描述 | ✅ | |
| 输出物要求 | ✅ | |

**问题**：
- 这个页面**缺少"协助人"字段**！只有"负责人"，没有"协助人"多选 ❌
- 负责人选择器正确加载了 `userList` ✅

---

## 八、独立变更管理页 (`change/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **查询/重置** | ✅ | 筛选功能 |
| **发起变更** | ✅ | 弹出创建弹窗 |
| **详情(点击行)** | ✅ | 打开抽屉 |
| **通过/驳回(待审批行)** | ❌ | `doApprove`调用 `updateApprovalRecord`，但 `approval/index.ts` 中此API路径是 `/pms/approval-record/update`，而变更应该调 `/pms/change-record/update`！**按钮调错API！** |
| **提交变更** | ✅ | 调 `createChangeRecord` |
| **提交审批** | ✅ | 调 `updateChangeRecord` |

**关键问题**：
- 列表中的"通过/驳回"按钮调用的是 `updateApprovalRecord`（审批API），但变更记录应该用 `updateChangeRecord`！**这是严重bug**！

---

## 九、独立审批管理页 (`approval/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **查询** | ✅ | 筛选功能 |
| **通过/驳回(待审批行)** | ⚠️ | 调 `updateApprovalRecord`，但**没有二次确认弹窗** |
| **详情(非待审批行)** | ❌ | `message.info('���看审批详情：...')` — 只是弹了个提示，没有实际详情页 |

---

## 十、独立文档管理页 (`document/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **上传文档** | ✅ | `el-upload` + 上传成功后调 `createDocument` |
| **查询** | ✅ | |
| **分类树点击** | ✅ | 按分类筛选 |
| **预览** | ⚠️ | 弹出预览弹窗，但内容区域显示 `el-empty` |
| **下载** | ✅ | `window.open(storagePath)` |
| **删除** | ✅ | 确认弹窗 + API |

---

## 十一、独立质量管理页 (`quality/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **新建质量问题** | ✅ | 弹出创建弹窗 |
| **批量关闭** | ✅ | 选中多行后批量调API |
| **查询/重置** | ✅ | |
| **详情(点击行)** | ✅ | 打开抽屉，含处理时间线和操作表单 |
| **提交(处理操作)** | ✅ | 状态流转：new→assigned→processing→pending_verify→closed |
| **确定(新建)** | ✅ | 调 `createQualityIssue` |

**问题**：
- 创建弹窗中**负责人**是 `el-input` 文本输入，不是用户选择器 ❌
- 独立页面 `quality/index.vue` 与详情页内嵌 `QualityTab.vue` 的严重度/状态映射**不一致**

---

## 十二、仪表盘 (`dashboard/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **导出** | ❌ | `handleExport` 函数体为空 |
| **时间范围切换** | ✅ | week/month/quarter/year |

---

## 十三、字典管理页 (`dict/index.vue`)

| 按钮 | 状态 | 说明 |
|------|------|------|
| **添加选项** | ✅ | 弹出表单 |
| **编辑** | ✅ | 弹出表单 |
| **删除** | ✅ | 确认弹窗 |
| **保存** | ✅ | 写入localStorage |

---

## 汇总：所有空壳/无效按钮

| 序号 | 页面 | 按钮 | 问题 |
|------|------|------|------|
| 1 | 项目列表 | "我负责的"��捷筛选 | 逻辑完全为空(TODO) |
| 2 | 项目列表 | "我参与的"快捷筛选 | 逻辑完全为空(TODO) |
| 3 | 项目列表 | "导出Excel" | `message.info('开发中')` |
| 4 | 项目详情 | "复制项目" | `message.info('开发中')` |
| 5 | 项目详情 | "导出项目" | `message.info('开发中')` |
| 6 | 任务列表Tab | "发起变更"(已完成任务) | emit事件父组件未监听 |
| 7 | 任务列表Tab | "我负责的"/"我参与的"筛选 | 逻辑为空 |
| 8 | 任务详情抽屉 | "编辑"按钮 | 只设flag无编���表单 |
| 9 | 任务详情抽屉 | "发起变更" | `message.info('开发中')` |
| 10 | 任务详情抽屉 | "上传文件"(输出物Tab) | `message.info('开发中')` |
| 11 | 任务详情抽屉 | "查看"(输出物Tab) | 按钮无实现 |
| 12 | 任务详情抽屉 | "提交审核"(审核Tab) | 不调API仅前端提示 |
| 13 | 变更管理列表 | "通过/驳回"(待审批) | **调错API！**应调change API却调了approval API |
| 14 | 审批管理列表 | "通过/驳回"(待审批) | **无二次确认弹窗** |
| 15 | 审批管理列表 | "详情"(非待审批) | `message.info`空壳 |
| 16 | 仪表盘 | "导出" | 函数体为空 |
| 17 | 甘特图Tab | 拖拽任务条 | 仅console.log不调API |

## 汇总：用户名显示为数字ID的位置

| 序号 | 文件 | 位置 | 代码 |
|------|------|------|------|
| 1 | `TaskListTab.vue:332` | 负责人列 | `'用户${task.mainOwnerId}'` |
| 2 | `project/index.vue:356` | 项目经理 | `'用户${project.projectManagerId}'` |
| 3 | `TaskDetailDrawer.vue:272` | 负责人 | `'用户${t.mainOwnerId}'` |
| 4 | `TaskDetailDrawer.vue:276` | 参与人 | `ids.map(id => '用户${id}')` |
| 5 | `GanttTab.vue:213` | 甘特图负责人 | `'用户${task.mainOwnerId}'` |
| 6 | `approval/index.vue:32` | 审批申请人 | `row.initiatorId`（直接显示数字） |
| 7 | `document/index.vue:48` | 上传人 | `row.uploadBy`（直接显示数字） |
| 8 | `change/index.vue:42-43` | 申请人/审批人 | `row.applicant/approver`（显示数字ID字符串） |

## 汇总：创建任务时缺少"关联人/协助人"字段

| 页面 | 有负责人 | 有协助人 | 说明 |
|------|----------|----------|------|
| 项目详情→创建任务弹窗 | ✅ | ✅ | 两者都有 |
| 独立任务管理→创建任务弹窗 | ✅ | ❌ | **缺协助人** |
| 项目创建向导→步骤3添加任务 | ✅ | ❌ | 只有roleName无真实用户选择 |

---

## 需要立即修复的 TOP 10

1. **变更管理列表"通过/驳回"调错API** — 致命bug，审批操作完全无效
2. **任务详情抽屉"编辑"按钮无效** — 用户点击后无任何反应
3. **任务详情抽屉"提交审核"不调API** — 审核操作丢失
4. **"发起变更"按钮全部无效**（TaskListTab + TaskDetailDrawer）
5. **"我负责的"/"我参与的"筛选空壳** — 核心UX功能
6. **全局用户名显示数字ID**（8处） — 用户无法识别人员
7. **独立任务管理缺少"协助人"字段**
8. **"导出Excel"/"复制项目"/"导出项目"空壳按钮应隐藏或禁用**
9. **审批管理"通过/驳回"无二次确认**
10. **TaskDetailDrawer缺少userList导致负责人/参与人显示数字ID**
